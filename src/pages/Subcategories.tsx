import React, { FC, ReactElement } from "react";

interface SubcategoriesProps {}

const Subcategories: FC<SubcategoriesProps> = ({}): ReactElement => {
  return <div>Subcategories list</div>;
};

export default Subcategories;
