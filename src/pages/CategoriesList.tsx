import React, { FC, ReactElement } from "react";

interface CategoriesListProps {}

const CategoriesList: FC<CategoriesListProps> = ({}): ReactElement => {
  return (
    <div>
      <div>Categories list</div>
    </div>
  );
};

export default CategoriesList;
